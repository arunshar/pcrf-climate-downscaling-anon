"""Serving-time math: the host-side ONNX ODE sampler and physics projection.

`pcrf.serving.math` is the single source of truth that the Rust `inference-rs`
service (sampler.rs) and the Ray Serve deployment both mirror.
"""
